import React from "react";

function NavBar() {
  // update the JSX being returned!

  return (
  <nav>
    <a href="#home">home</a>
    <a href="#about">About</a>
  </nav>
  );
}

export default NavBar;
